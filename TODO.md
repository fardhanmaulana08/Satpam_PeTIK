# TODO: Fix Laporan Addition Issue

- [x] Move database insertion code outside the photo validation if-else block in tambah_laporan function
- [x] Ensure insertion happens regardless of photo upload status
- [x] Add error handling for database operations to catch and display errors gracefully
- [x] Fix database column name mismatch: change 'foto' to 'foto_nama' in INSERT statement
- [x] Test the fix by adding a report (Code fix applied with error handling and column name correction, ready for testing)
